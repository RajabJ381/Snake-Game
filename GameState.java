/**
 * The high-level phases the game moves through. SnakeGame renders and
 * responds to key presses differently depending on which state it's in.
 */
public enum GameState {
    START_SCREEN,
    PLAYING,
    PAUSED,
    GAME_OVER
}
